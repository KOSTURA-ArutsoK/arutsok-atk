import { AddCompanyCard } from "@/components/AddCompanyCard";

export default function PridatStavZmluvy() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <AddCompanyCard onClick={() => {}} />
    </div>
  );
}
